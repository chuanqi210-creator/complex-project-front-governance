# 前置治理协议发布包 2026-06-22

用途：把宏观重构后的前置治理协议、低摩擦入口、真实项目经验库、路由表、压力测试产物和恢复入口整理成可复用结构。当前版本已从“场景覆盖式 trace 目录”升级为“新领域构造式治理内核”，并将第 5 节阶段流程升级为 `dynamic_stage_controller` 动态调度。

## 一、从这里开始

用户入口：

`/Users/chuchenqidawang/Documents/ai 科研/复杂项目启动_低摩擦用户入口_20260622.md`

推荐用户只说：

```text
这是一个重大项目。
目标是：……
已有材料在：……
我希望结果达到：……
请按前置治理协议推进，但不要把流程负担转嫁给我。
```

AI 应做：

1. 先启用 `dynamic_stage_controller`，判断 light / standard / deep / continuous 深度和 route_event，并用 `routing_decision_log` 记录未选路线、拒绝理由、触发证据和用户摩擦影响。
2. 自动识别最终要证明的主张。
3. 启用 `new_domain_governance_builder`，生成本轮临时领域 trace。
4. 用 `category_experience_router` 选择一个主类和必要的 high_risk_regulatory 标签。
5. 从经验库索引抽取 1-3 个相近失败模式校准，而不是复制旧 trace 目录。
6. 用 `agentic_orchestration_capability_builder` 判断是否需要学习 skill/plugin/外部方法、是否有可调用子代理能力、上下文包和 review gate 是否准备好。
7. 用 `agent_topology_selection_trace` 判断本轮应采用 single_agent、agents_as_tools、handoff、parallel_evidence_lanes、orchestrator_workers、reviewer_loop、group_chat 还是 user_proxy，并写清控制权、状态边界、上下文工程、成本收益和降级路线。
8. 用 `subagent_orchestration_pattern_router` 判断单代理是否足够、应选择哪种编排模式、拒绝了哪些模式，以及外部方法如何映射为 work unit。
9. 用 `subagent_orchestration_ladder` 判断是否需要只读审计、并行探索、worker、reviewer 或 inline_fallback。
9. 最多问 1-2 个高杠杆问题，其余治理写入文件。

## 二、核心协议文件

| 文件 | 用途 |
| --- | --- |
| `/Users/chuchenqidawang/Desktop/复杂项目启动前置治理协议_v3_核心版.md` | 主协议，包含通用治理内核、阶段流程、工具型闸门、新领域构造器和反膨胀机制 |
| `/Users/chuchenqidawang/Documents/ai 科研/前置治理协议_真实项目经验库索引_20260622.md` | 独立经验库索引，承接旧具体领域 trace 和真实项目压力测试经验 |
| `/Users/chuchenqidawang/Documents/ai 科研/真实项目压力测试_跨域收束与低摩擦路由表_20260622.md` | 新领域构造式路由表 |
| `/Users/chuchenqidawang/Documents/ai 科研/新领域治理构造器_三类模拟回归_20260622.md` | 三类陌生项目模拟回归，验证不新增具体 trace 目录也能深入治理 |
| `/Users/chuchenqidawang/Documents/ai 科研/前置治理协议_二十个跨渠道项目逆向校验实验.md` | 持续压力测试和机器看版 |
| `/Users/chuchenqidawang/Documents/ai 科研/持续优化变更清单_20260622.md` | 本轮触碰文件、验证状态、恢复入口 |

## 三、当前协议能力

主协议主体保留的是跨领域能力，不再保留具体领域长目录：

1. `compressed_user_entry`
2. `current_basis` / `resource_boundary`
3. `evidence_matrix`
4. `decision_log`
5. `traceability_matrix`
6. `deliverable_execution_trace`
7. `live_project_pool_scan`
8. `real_project_pressure_test_gate`
9. `new_domain_governance_builder`
10. `category_experience_router`
11. `trace_module_router`
12. `rule_promotion_mechanism`
13. `micro_task_execution_check`
14. `superpower_subagent_orchestration`
15. `source_link_recency_audit`
16. `rendered_artifact_freshness`
17. `numeric_calculation_trace`
18. `append_only_eof_guard`
19. `latest_board_tail_check`
20. `release_package_freshness`
21. `change_inventory`
22. `anti_protocol_bloat_gate`
23. `dynamic_stage_controller`
24. `stage_depth_ladder`
25. `subagent_orchestration_ladder`
26. `subagent_task_contract`
27. `routing_decision_log`
28. `current_scope_recovery_scan`（含 `ambiguous_archive_hits` 显性标签检查）
29. `subagent_capability_probe`
30. `subagent_lifecycle_cleanup`
31. `append_eof_section_tool`
32. `verify_governance_recovery_tool`（连续自我优化默认使用 `--preset continuous-self-optimization`）
33. `render_qa_state_consistent`（交付物渲染阻塞修复后，验证当前 QA 状态和页图/PDF 证据一致）
34. `subagent_problem_decomposition_builder`（调用子代理前先拆分工作包、上下文包、验收证据、审查流水线和生命周期账本）
35. `source_role_map`（外部来源先标明证据角色，防止把项目存在、规则或报道外推为效果证据）
36. `verifier_required_field_drift`（主协议晋升核心字段后，恢复链 preset 必须同步 required/forbid 检查；验证器 required 字段必须由共享核心源与 scope extras 生成，测试锁住生成方式和 scope extras 精确匹配）
37. `skill_plugin_learning_loop`（用户要求学习网上方法、skill 或插件时，记录发现、读完、硬约束、小题试用、采纳、写回、验证器和恢复链引用）
38. `skill_plugin_learning_loop_gap`（防止只写“参考了 skill/plugin”却没有形成下一次可复用能力）
39. `skill_plugin_project_fit_trace`（把 skill/plugin 学习绑定到当前真实项目的主张、缺口、前后差异、验收证据和不能迁移边界）
40. `skill_plugin_detached_from_project_gap`（防止工具学习闭环自洽但项目本身没有变得更可执行）
41. `source_authority_precedence_trace`（多来源覆盖同一规则、日期、程序或事实主张时，记录谁优先、优先范围、冲突裁决和降级规则）
42. `source_authority_flattening_gap`（防止把摘要页、时间线、宣传材料、邮件或二手报道压平为同等权威）
43. `subagent_method_learning_trace`（学习网上方法、skill 或插件提升子代理能力时，形成独立问题域、上下文隔离、handoff/guardrail、review/integration 的可执行分工规则）
44. `subagent_method_name_drop_gap`（防止只堆外部框架名或平台名，没有形成当前项目可验证的子代理编排能力）
45. `subagent_method_learning_field_coverage`（`subagent_method_learning_trace` 已纳入恢复链 preset required，缺字段时 verifier 必须红灯）
46. `parallel_evidence_lane_trace`（根因/修复耦合但证据源可独立核验时，只并行只读证据泳道，最终裁决由主代理整合）
47. `claim_lane_binding`（证据泳道必须绑定主张层级，防止用活动量、产出或质量证据外推真实使用、结果和长期影响）
48. `deployment_readiness_gate`（从 benchmark/demo/论文指标/离线评估上升到真实使用或部署时，必须补外部有效性、工作流、风险授权、监管政策和监控承接证据）
49. `best_practice_learning_contract`（学习网上方法、官方资料、行业最佳实践或 agent 编排方法时，必须绑定阶段缺口、抽取方法、失败模式、应用测试和不可迁移边界）
50. `capability_type_and_side_effect_gate`（区分 skill/plugin/connector/local_tool/web_source/external_method 的操作模式、副作用和授权边界）
51. `external_state_write_guard`（可能写入外部系统或改变账号/课程/仓库/成绩/名单/发布状态时，记录权限范围、目标对象、敏感数据、子代理能力边界、沙盒/干跑、回滚和用户授权）
52. `skill_plugin_discovery_gate`（触发 skill/plugin/tool 时记录候选集、选择/拒绝理由、读取证据和调用/跳过决定）
53. `integration_lifecycle_gate`（集成/互通/兼容/生产部署主张必须检查同版本端到端证据与维护/退役/迁移/支持信号，禁止单边标准能力相加为组合可用）
54. `subagent_result_coverage_gate`（子代理返回后逐项核对 contract 覆盖、未验证项、越界项和主代理补查项）
55. `main_agent_integration_review`（主代理记录冲突、采纳/拒收、最终主张变化和降级理由）
56. `claim_readiness_ladder`（最终主张绑定 idea/source/local/small-loop/pilot/production-public 层级，防止越级表达）
57. `transactional_integration_consistency_guard`（异步外部副作用、交易、webhook、回调或可重试请求必须检查幂等、去重、重试、乱序、权威状态源、对账、test/live 边界和高影响动作补偿）
58. `agentic_orchestration_capability_builder`（学习网上方法、skill、插件或提升子代理能力时，先构造能力缺口、来源组合、`method_transfer_matrix`、Codex 工具探测、上下文包、验收证据、review gate、主代理集成和生命周期清理）
59. `agent_topology_selection_trace`（学习外部 agent/skill/plugin 方法后，先决定 single_agent、agents_as_tools、handoff、parallel_evidence_lanes、orchestrator_workers、reviewer_loop、group_chat 或 user_proxy，并记录最终答案控制权、状态写入边界、上下文包、依赖图、成本收益、降级路线和小题验证）
60. `subagent_orchestration_pattern_router`（学习外部 agent/skill/plugin 方法后，先判断单代理是否足够，再选择编排模式，并记录 rejected_patterns、method_to_work_unit_mapping、role_selection_reason、context_packet_completeness 和 minimum_application_test）
61. `desired_state_reconciliation_guard`（Terraform/IaC/GitOps/Kubernetes controller 等声明式期望状态系统必须检查 desired source、observed state、plan/diff、drift、锁/单写者、破坏性动作、import/adoption、policy gate、rollback/forward fix 和 operator handoff）
62. `stateful_data_migration_guard`（数据库 schema、索引/约束、字段重命名、类型变更、回填或数据模型重构必须检查兼容窗口、expand/backfill/cutover/contract、在线 DDL、回填校验、回滚边界、观测暂停恢复和 owner runbook）
63. `security_incident_response_guard`（安全漏洞、凭证泄露、扫描告警、PoC、issue/PR、CVE/GHSA/advisory、协调披露或真实安全事件主张必须区分线索、可利用性、已利用、已入侵、已外泄、修复验证和披露通知状态）
64. `data_artifact_lineage_freshness_guard`（数据集/报表/模型/图表/指标/分析输出支撑结论、下游引用或决策时，绑定来源快照、刷新时间、转换逻辑、质量验证、复现配方、下游引用和 stale 降级；每个关键字段必须标注 `field_evidence_status`，防止字段勾选替代证据）
65. `method_transfer_matrix`（学习外部 agent 方法、skill、plugin 或工具时，把来源角色、可迁移方法、fit/reject 信号、生成的 work unit、最小应用测试、验收证据、不能外推边界和降级路线写清楚）
66. `gate_activation_matrix`（把重大项目常驻检查按 always_minimum、risk_based、continuous_only、recovery_only 动态激活，避免全量硬跑或低摩擦误跳过）
67. `read_only_audit_subagent_contract`（只读审计子代理禁止编辑、安装、认证、外部写入、Plan/Goal 操作和最终裁决；输出锚点、缺口、不能支持主张、建议补法和主代理补查项）
68. `capability_resolution_order`（区分 active local skill/tool、tool discovery、instruction read、side effect gate、install/auth/write authorization；environment_listed 不等于 callable）
69. `schema_hygiene_gate`（防止 YAML/tab/重复编号/alias 漂移被后续代理复制；示意结构必须标注 schematic only）

旧具体 trace 例如工程原型、开源贡献、支教服务、食品安全、交通运行、心理健康、文化遗产等，均通过经验库索引回查，不再作为主协议主体目录。

## 四、经验库定位

经验库索引：

`/Users/chuchenqidawang/Documents/ai 科研/前置治理协议_真实项目经验库索引_20260622.md`

经验库保留：

1. `trace_id`
2. `primary_library`
3. `risk_tags`
4. `failure_mode_id`
5. `source_pressure_test_file`
6. `current_as_of`
7. `reusable_method`
8. `non_transferable_detail`

经验库不做：

1. 不替代新项目的行业最佳实践调查。
2. 不自动展开旧 trace 长模板。
3. 不把历史案例当作当前事实。
4. 不让用户从长目录里选择。

## 五、恢复入口

最新机器看版：

`/Users/chuchenqidawang/Documents/ai 科研/前置治理协议_二十个跨渠道项目逆向校验实验.md`

搜索文本：`## 214. 当前机器看版`

当前 next_route：`continue_self_optimization_with_next_real_project_builder_or_software_delivery_state_boundary_regression`

## 六、交付物 QA 状态

`/Users/chuchenqidawang/Documents/ai 科研/四川省平昌县第二中学支教.docx` 的历史视觉渲染阻塞已在 2026-06-22 修复：本地 Codex 缓存中的 headless LibreOffice 依赖闭包已补齐并保留备份，随后用 `render_docx.py` 重新渲染。

当前 QA：`rendered_pass`，视觉渲染抽检通过。

渲染证据：

1. 输出目录：`/Users/chuchenqidawang/Documents/ai 科研/render_pingchang_check`
2. 页图：`page-1.png` 到 `page-25.png`，共 25 页。
3. PDF：`/Users/chuchenqidawang/Documents/ai 科研/render_pingchang_check/四川省平昌县第二中学支教.pdf`
4. 抽检页：封面 `page-1.png`、正文 `page-14.png`、评价体系表 `page-22.png`、参考资料 `page-25.png`。
5. 残留 warning：`Fontconfig error: Cannot load default config file`，未阻止 PDF/PNG 输出，抽检页未见可见版面损坏。

## 七、下一轮建议

下一轮应在恢复链通过后继续验证“动态阶段控制器 + 新领域构造式治理内核”是否能在不新增具体 trace 目录的情况下处理陌生项目，或继续选择一个真实交付物做可用性回归：

1. 陌生工程/软件/产品项目。
2. 陌生社会实践/公共服务项目。
3. 陌生研究/数据/模型项目。

每个项目都必须输出：mode、stage_depth_budget、route_event、routing_decision_log、主张、类别路由、选中镜头、小题证据、子代理策略、失败模式、降级表述、不能外推边界和经验库引用。
