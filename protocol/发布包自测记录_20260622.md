# 发布包自测记录 2026-06-22

目的：验证只读取 `/Users/chuchenqidawang/Documents/ai 科研/前置治理协议发布包_20260622.md`，是否能恢复当前状态，而不需要通读长日志。

## 一、自测方法

只扫描发布包中的恢复入口、current next_route、未完成 QA 和核心文件清单。

检查命令：

```text
rg -n "## [0-9]+\\. 当前机器看版|next_route|未完成 QA|恢复入口" 前置治理协议发布包_20260622.md
```

## 二、自测发现

自测未通过。

发布包仍指向：

```text
## 58. 当前机器看版
protocol_release_package_consolidation
```

但真实最新日志已经推进到：

```text
## 60. 当前机器看版
release_package_self_test
```

## 三、失败模式

```yaml
failure_id: F41-01
name: release_package_stale
symptom: 发布包存在，但恢复入口、最新机器看版或 next_route 指向旧状态
root_cause: 发布包生成后，日志和 next_route 继续推进，但发布包未同步更新
corrected_behavior: 任何更新最新机器看版、next_route、关键文件、未完成 QA 或恢复入口后，必须执行 release_package_freshness 检查
regression_case: R41-release-package-freshness
```

## 四、协议修复

已写回桌面协议：

1. 总规则新增第 39 条：存在发布包、恢复包、handoff 文档或对外交接清单时，必须检查 `release_package_freshness`。
2. 执行前闸门新增第 98 条。
3. 失败模式库新增 `release_package_stale`。

## 五、修复标准

发布包必须能定位：

1. 最新机器看版。
2. 当前 next_route。
3. 核心协议文件。
4. 低摩擦入口。
5. 路由表。
6. 渠道库。
7. 变更清单。
8. 未完成 QA。

如果发布包不是最新状态，恢复入口必须降级为 stale，不得作为唯一 handoff 依据。
