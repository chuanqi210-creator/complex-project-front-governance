# 操作记录

## 2026-06-23

- 从 `/Users/chuchenqidawang/Documents/ai 科研` 中拆出“复杂项目启动前置治理协议”相关材料。
- 整理 `protocol/`、`tools/`、`outputs/front_governance_protocol_v2/` 等核心目录。
- 初始化为独立 Git 项目，默认分支为 `main`。
- 补充 `README.md` 与 `AGENTS.md`，用于 Codex 后续低摩擦协作。
- 补充 `来源记录.md`，明确来源路径和正式项目边界。
- 已验证可以从 `main` 创建临时工作树 `.worktrees/codex-smoke-20260623`，验证后已删除临时工作树和临时分支。
- 验证命令：`python3 tools/test_verify_governance_recovery.py`。
- 验证结果：`ok`。
- 按用户要求进行独立化整理：本仓库成为当前权威工作区，旧 `/Users/chuchenqidawang/Documents/ai 科研` 降级为历史来源。
- 已将 Desktop v3 主协议复制为 `protocol/复杂项目启动前置治理协议_v3_核心版.md`。
- 已将经验库引用和第 150-214 节恢复链相关的历史回归记录同步到 `docs/history/`。
- 已新增 `docs/migration/独立化迁移清单_20260623.md`，记录权威路径、历史来源和同步范围。
- 已将 `tools/verify_governance_recovery.py` 默认路径切换到本仓库，并新增默认路径回归测试。
- 独立化验证命令：`python3 tools/test_verify_governance_recovery.py`，结果：`ok`。
- EOF 扫描命令：`python3 tools/append_eof_section.py scan protocol/前置治理协议_二十个跨渠道项目逆向校验实验.md --latest-heading "## 214. 当前机器看版"`，结果：最新看版在 EOF，`pseudo_heading_count: 0`，`latest_pending_count: 0`。
- 综合恢复链命令：`python3 tools/verify_governance_recovery.py --preset continuous-self-optimization --latest-heading "## 214. 当前机器看版" --expected-route continue_self_optimization_with_next_real_project_builder_or_software_delivery_state_boundary_regression`，结果：`failure_count: 0`，默认 root 为本仓库。
